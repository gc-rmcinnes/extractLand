After knitting and rendering `extractLand.Rmd`, overwrite this file with a symlink to `extractLand.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/rmcinnes/Documents/prepLocTracks/extractLand
rm README.md && ln -s extractLand.md README.md
```
